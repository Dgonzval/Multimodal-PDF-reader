# V3 requirements (updated from requirements.txt)
# Install with: pip install -r requirements_v3.txt

pymupdf                   # pdf image/text extraction
python-dotenv==1.0.0
streamlit

# OpenAI
openai

# LangChain (updated modular packages)
langchain>=0.3.0
langchain_openai>=0.3.0
langchain-community>=0.3.0
langchain-chroma          # replaces the old langchain.vectorstores.Chroma
langchain-text-splitters

# Vector store
chromadb

# PDF text extraction
PyPDF2

# ML utilities
numpy
scikit-learn

# Ngrok tunnel (public URL for local Streamlit app)
pyngrok
